#include <iostream>
#include <string>

using namespace std;

int main() {
    setlocale(LC_ALL, "Russian");
    cout << "привет, это Калькулятор больших вещественных чисел, сначала напиши двоичное число с которым хочешь совершить опперацию" << endl;
    string first, second, result, maxi, mini;
    bool perenos = false;
    cin >> first;
    if (first [0] != '1' && first [0] != '0')
    {
        cout<< "ОШИБКА" << endl <<"вводите только двоичное число" << endl;
        return 0;
    }
    cout << "теперь выбери что хочешь сделать с этим числом"<< endl << "[1] - сложить" << endl << "[2] - вычесть" << endl << "[3] - умножить" << endl;
    int choose;
    cin >> choose;
    if (choose == 1)
    {
        cout << "теперь можно написать второй число, которое ты хочешь сложить: ";
        cin >> second;
        if (first [0] != '1' && first [0] != '0')
        {
            cout<< "ОШИБКА" << endl <<"вводите только двоичное число" << endl;
            return 0;
        }

        if (first.size() >= second.size())
        {
            maxi = first;
            mini = second;
        }
        else
        {
            mini = first;
            maxi = second;
        }
        result = maxi;
        int i = 0;
        for(i; i < maxi.size(); i++)
            if(maxi[maxi.size()  - i] == 0 && mini[mini.size()  - i] == 0 && perenos == false) result[maxi.size()  - i] == 0;
            else if (maxi[maxi.size()  - i] == 0 && mini[mini.size()  - i] == 0 && perenos == true){
               result[maxi.size()  - i] == 1;
               perenos = false;
            }
            if(maxi[maxi.size()  - i] == 1 && mini[mini.size() - i] == 0 && perenos == false || maxi[maxi.size()  - i] == 0 && mini[mini.size()  - i] == 1 && perenos == false) result[maxi.size()  - i] == 1;
            else if(maxi[maxi.size() - i] == 1 && mini[mini.size() - i] == 0 && perenos == false || maxi[maxi.size() - i] == 0 && mini[mini.size() - i] == 1 && perenos == true) result[maxi.size()  - i] == 1;
            {
                if (mini[mini.size() - i] == 0) mini[mini.size() - i] = 1;
                else maxi[maxi.size() - i] = 1;

            }
            if(maxi[maxi.size() - i] == 1 && mini[mini.size() - i] == 1)
            {
                result[maxi.size() - i] == 0;
                perenos = true;
            }
исправь неточности в коде
